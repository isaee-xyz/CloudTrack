rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // For now, allowing all access as per our previous discussion.
      // In production, we should restrict this to authenticated users.
      allow read, write: if true;
    }
  }
}
